name: "extender:stage channel:stable"

on:
  schedule:
    - cron: 0 11 * * *

jobs:
  test:
    uses: ./.github/workflows/test
    with:
      channel: stable
    secrets:
      slack_webhook: ${{ secrets.SLACK_WEBHOOK }}
